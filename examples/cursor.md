# Cursor MCP Example

Use the same command Cursor expects for a local MCP server:

```json
{
  "apeiron": {
    "command": "python",
    "args": ["-m", "apeiron.api.mcp_server"],
    "cwd": "/path/to/apeiron"
  }
}
```
